% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggrank-data.R
\name{ggrank_data}
\alias{ggrank_data}
\title{Prepare and inspect ranked data}
\usage{
ggrank_data(
  data,
  category,
  period,
  value = NULL,
  rank = NULL,
  label = NULL,
  group = NULL,
  periods = NULL,
  direction = c("descending", "ascending"),
  ties = c("min", "dense", "first"),
  check_rank = TRUE
)
}
\arguments{
\item{data}{A data frame with one row per category and period.}

\item{category, period}{Unquoted columns identifying the category and state.}

\item{value}{Optional unquoted numeric ranking-value column. It may be
omitted when an authoritative \code{rank} column is supplied.}

\item{rank}{Optional unquoted column containing authoritative precomputed
ranks. When supplied, these ranks are preserved.}

\item{label}{Optional unquoted display-label column.}

\item{group}{Optional unquoted category-group column.}

\item{periods}{Optional vector selecting and ordering states. Unlike
\code{\link[=ggrank]{ggrank()}}, this preparation helper is not limited to four states.}

\item{direction}{Whether large (\code{"descending"}) or small (\code{"ascending"})
values rank first.}

\item{ties}{Ranking method: \code{"min"} (competition ranking), \code{"dense"}, or
\code{"first"} (unique ranks resolved alphabetically by category).}

\item{check_rank}{When \code{TRUE}, supplied ranks are checked for potential
disagreements with the values and ranking direction. These checks warn
rather than fail because authoritative ranks may use external information.}
}
\value{
A data frame containing \code{category}, \code{period}, \code{value}, \code{rank},
\code{display_position}, \code{label}, and \code{group}.
}
\description{
Calculates ranks within each state before any top-N display filtering is
applied. This helper is optional: \code{\link[=ggrank]{ggrank()}} performs the same ranking
automatically. Use it when you want to inspect, teach, export, or reuse the
calculated ranks.
}
\details{
Ranking uses the exact numeric \code{value}; formatting supplied through \code{label}
never changes the rank. By default, equal values receive the same competition
rank (\verb{1, 2, 3, 3, 5}). Tied categories receive separate alphabetical display
positions so their plot boxes do not overlap.
}
\examples{
ggrank_data(ggrank_products, product, year, sales)

tied <- data.frame(
  year = rep(c(2020, 2025), each = 4),
  organism = rep(LETTERS[1:4], 2),
  rate = c(5, 4, 3, 3, 6, 4, 4, 2)
)
ggrank_data(tied, organism, year, rate)

ranks_only <- data.frame(
  year = rep(c(2024, 2025), each = 3),
  student = rep(c("A", "B", "C"), 2),
  rank = c(1, 2, 3, 2, 1, 3)
)
ggrank_data(ranks_only, student, year, rank = rank)
}
