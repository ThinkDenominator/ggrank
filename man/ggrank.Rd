% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggrank.R
\name{ggrank}
\alias{ggrank}
\title{Draw a rank-transition chart}
\usage{
ggrank(
  data,
  category,
  period,
  value = NULL,
  rank = NULL,
  label = NULL,
  group = NULL,
  periods = NULL,
  top_n = 10,
  direction = c("descending", "ascending"),
  ties = c("min", "dense", "first"),
  show_transitions = c("boundary", "top_only", "all"),
  colour_by = c("auto", "group", "movement", "none"),
  palette = NULL,
  legend_title = NULL,
  legend_labels = NULL,
  show_legend = TRUE,
  category_header = "Category",
  value_header = "Value",
  label_wrap = 28,
  category_width = 2.4,
  value_width = 1.55,
  state_gap = 1.15,
  base_size = 11,
  title = NULL,
  subtitle = NULL,
  check_rank = TRUE
)
}
\arguments{
\item{data}{A data frame with one row per category and period.}

\item{category, period}{Unquoted columns identifying the category and ordered
state.}

\item{value}{Optional unquoted numeric ranking-value column. It may be
omitted when an authoritative \code{rank} column is supplied.}

\item{rank}{Optional unquoted column containing precomputed ranks.}

\item{label}{Optional unquoted display-label column. By default \code{value} is
formatted using \code{format()}.}

\item{group}{Optional unquoted category-group column.}

\item{periods}{Optional vector selecting and ordering two to four states.}

\item{top_n}{Rank threshold to retain in each state. All categories tied at
the boundary are included, so the result may contain more than \code{top_n}
categories.}

\item{direction}{Whether large (\code{"descending"}) or small (\code{"ascending"})
values rank first.}

\item{ties}{Ranking method: \code{"min"} (the default competition ranking),
\code{"dense"}, or \code{"first"} (unique alphabetical ranks).}

\item{show_transitions}{\code{"boundary"} retains categories appearing in the
top N in any selected state; \code{"top_only"} includes top-N observations
only; \code{"all"} includes every category.}

\item{colour_by}{Colour categories by \code{"group"}, \code{"movement"}, or \code{"none"}.
The default \code{"auto"} uses groups when supplied and movement otherwise.}

\item{palette}{Optional named colour vector.}

\item{legend_title}{Optional legend title.}

\item{legend_labels}{Optional named labels corresponding to every displayed
group or movement value.}

\item{show_legend}{Show the colour legend.}

\item{category_header, value_header}{Headers shown over the two box columns.}

\item{label_wrap}{Approximate number of characters per category-label line.}

\item{category_width, value_width}{Relative widths of the category and value
boxes. Increase \code{value_width} for long confidence-interval labels.}

\item{state_gap}{Horizontal space reserved for connectors between states.}

\item{base_size}{Base text size passed to \code{\link[=theme_ggrank]{theme_ggrank()}}.}

\item{title, subtitle}{Optional plot title and subtitle.}

\item{check_rank}{When \code{TRUE}, supplied ranks are checked for disagreements
with equal values, shared ranks across different values, and the requested
ranking direction. Potential disagreements warn rather than fail because
authoritative ranks may use external tie-breakers or additional data.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a \code{ggplot2} chart that combines ranked tables with connecting lines.
Categories entering or exiting \code{top_n} remain visible outside the boundary.
}
\details{
With \code{colour_by = "auto"}, supplying \code{group} uses group colours; otherwise
movement colours are used. Movement classification prioritises entry to and
exit from the selected top-N boundary, followed by positive (\code{riser}),
negative (\code{faller}), or zero (\code{stable}) rank change. Palette and legend-label
names must match the displayed group or movement values exactly.
}
\examples{
ggrank(ggrank_causes, cause, year, rate,
  rank = rank, label = display_value, group = cause_group,
  periods = c(1990, 2021), top_n = 10
)
}
